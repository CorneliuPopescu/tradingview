# vendored MCP server, cloned from https://github.com/tradesdontlie/tradingview-mcp
tradingview-mcp/

# session memory (remember plugin)
.remember

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
